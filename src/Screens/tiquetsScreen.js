import React from 'react';
import { View, Text } from 'react-native';

export function TiquetsScreen() {
	return (
		<View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
			<Text>Tiquets Screen</Text>
		</View>
	);
}